# ansible
alias ans='docker start ansible && docker attach ansible'
alias dan='docker run --rm --name=ansible --net=host -it -v ~/Projects/ansible:/etc/ansible -v ~/.ssh:/root/.ssh -w /etc/ansible netw0rm/ansible sh'
